// articles reducer
const articlesReducerDefaultState = {}

export default (state = articlesReducerDefaultState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};